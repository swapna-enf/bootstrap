echo "Setting up _MY's environment"

cp ~/.bashrc.orig ~/.bashrc
echo "alias sal='source ~/bootstrap/.mybash.rc'" >> ~/.bashrc

mkdir ~/work

sudo apt-get update
sudo apt-get install git vim mosh -y

git config --global user.name "Kesavan K Mannar"
git config --global user.name "Kesavankm.my@gmail.com"

cp ~/bootstrap/.vimrc ~/
cp ~/.tmux.conf ~/
